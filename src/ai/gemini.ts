'use server';

// AI functionality is disabled as per user request.

/**
 * Generates a structured answer using the Gemini API.
 * This function is currently disabled.
 */
export async function generateAiAnswer(prompt: string) {
  console.error("AI functionality is currently disabled.");
  throw new Error("AI functionality is not available at the moment.");
}
